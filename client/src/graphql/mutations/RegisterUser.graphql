mutation RegisterUser($input: NewUserInput!) {
  register(input: $input) {
    user {
      id
      username
      firstName
      lastName
      email
    }
    errors {
      field
      message
    }
  }
}
